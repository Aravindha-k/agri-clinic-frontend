# Use Vite proxy in dev (see vite.config.js) — requests go to same origin, proxied to Django.
VITE_API_BASE_URL=/api/v1/

# Or point directly at Django (requires CORS on backend):
# VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1/

VITE_API_TIMEOUT=30000
