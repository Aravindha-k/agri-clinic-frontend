# AWS production build — baked into Vite bundle at build time
# Same host as mobile app. HTTPS is not currently available on this host
# (TLS handshake fails); production API is HTTP until cert is provisioned.
VITE_API_BASE_URL=http://13.207.17.117

VITE_API_TIMEOUT=30000
